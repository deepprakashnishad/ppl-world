import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aesm.goodact',
  appName: 'Good-Act',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
