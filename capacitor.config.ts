import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aesm.girirajmart',
  appName: 'GirirajMart',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
